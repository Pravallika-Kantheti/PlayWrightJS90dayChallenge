//What is a Function?
//A function is a **reusable block of code that performs a specific task.**
//Instead of repeating logic, you define it once and call it whenever needed.

//Function** = standalone callable.

//Method** = function stored as object property (called via object). **OOPs - Class, Objects, Typescript**


// **There are four types of functions which exist (JS)**

// -  no return type. No arguments 
// - with return type No arguments.
// -  with argument, no return
// -  with arguments and with return 

//define
function greet() {
    console.log("Hello Testing Academy!");
}

//calling
greet();